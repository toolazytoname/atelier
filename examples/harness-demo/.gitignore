# Generated by the orchestrator at runtime. The repo only ships the
# spec, the driver, and the README — the actual code the generator
# produces (demo/) and the score cards live outside source control.
demo/
score-cards/
__pycache__/
*.pyc
*.pyo
.pytest_cache/