{
  "mcpServers": {
    "atelier": {
      "type": "stdio",
      "command": "python3",
      "args": ["${CLAUDE_PROJECT_DIR}/bin/mcp-atelier.py"],
      "env": {}
    }
  },
  "_about": "Atelier project MCP config. One stdio bridge: atelier — a thin Python stdlib wrapper over `bin/devbox --json` so an agent can drive the sandbox (run/status/doctor/run_claude/version) without shelling out. CC running inside the VM loads this automatically."
}
